# Úsalo solo si un Nginx externo necesita llegar al gateway por el host.
# Dokploy Native Domains no requiere este archivo: enruta directamente al
# puerto interno 80 del servicio `nginx`.
services:
  nginx:
    ports:
      - "${NGINX_BIND_ADDRESS:-127.0.0.1}:${NGINX_PORT:-8080}:80"
